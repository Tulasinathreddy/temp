/*CMD
  command: /admintg
  help: 
  need_reply: true
  auto_retry_time: 
  folder: 
  answer: Password
  keyboard: 
  aliases: 
CMD*/

// Automatic fix
var msg;

// Automatic fix
var msg;

// Automatic fix
var msg;

msg = "Access denied. Password is: `123` /login"

if(message=="ramniwas899"){
  Bot.setProperty("admin_chat", chat.chatid, "string")
  msg = "You admin now. Please wait messages from users";
}

Bot.sendMessage(msg);

